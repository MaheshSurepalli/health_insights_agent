apiVersion: v1
kind: Service
metadata:
  name: hia-frontend
  namespace: {{ .Values.global.namespace }}
  labels: { app: hia-frontend }
spec:
  type: ClusterIP
  selector: { app: hia-frontend }
  ports:
    - name: http
      port: {{ .Values.frontend.service.port }}
      targetPort: 80
